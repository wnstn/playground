// Want to play around with React.js? Sweet, it's easy to setup.
// Use the `gulp create` task to make a new view, and add an element
// to that view with an ID of your choosing. Put that below where it says
// "your element id here". Next, hit up the React Tutorial here
// https://facebook.github.io/react/docs/tutorial.html
// to start writing your own components. If you have the server running
// saves to this file should auto compile and instantiate into the browser
// assuming your browser is open to http://localhost:4000/[filename]

(function(){
  // var React = require('react'),
  //     ReactDOM = require('react-dom'),
  //     MyComponent = require('./[your filename].js');

  // YOUR CUSTOM COMPONENTS HERE
  // HAVE FUN

  // var app = document.getElementById(your element id here);

  //if (app) {
  //   console.log('App view is live');
  //   ReactDOM.render(
  //       <MyComponent />,
  //       document.getElementById('app')
  //     );
  // }

})()
